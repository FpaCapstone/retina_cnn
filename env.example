# Backend API Configuration
# Copy this file to .env and update with your values

# ===================================
# Option 1: Explicit Backend URL (Single Backend)
# ===================================
# If set, only this URL will be used (no automatic fallback)
# Uncomment and set one of these:

# Local backend (for development)
# EXPO_PUBLIC_API_BASE_URL=http://localhost:3000

# Local network backend (for mobile syncing - replace with your computer's IP)
# EXPO_PUBLIC_API_BASE_URL=http://192.168.1.100:3000

# Render backend (production)
# EXPO_PUBLIC_API_BASE_URL=https://retina-cnn.onrender.com

# ===================================
# Option 2: Automatic Fallback (Recommended)
# ===================================
# Leave EXPO_PUBLIC_API_BASE_URL unset, and configure:
# - Local backend URL (for development/syncing)
# - Render backend will be used as fallback automatically

# Local backend URL (for development/syncing from phone)
# Replace 192.168.1.100 with your computer's local IP address
# EXPO_PUBLIC_LOCAL_BACKEND_URL=http://192.168.1.100:3000

# How to find your local IP:
# - macOS/Linux: ifconfig | grep "inet " | grep -v 127.0.0.1
# - Windows: ipconfig
# - Look for something like: 192.168.1.100 or 10.0.0.5

# ===================================
# RETFound API Configuration (optional)
# ===================================
# Development (local)
EXPO_PUBLIC_RETFOUND_API_URL=http://localhost:5000

# Production (update when deployed)
# EXPO_PUBLIC_RETFOUND_API_URL=https://your-retfound-api.com

# Optional: Enable/disable RETFound preference
# EXPO_PUBLIC_PREFER_RETFOUND=true
# EXPO_PUBLIC_FALLBACK_TO_CNN=true
